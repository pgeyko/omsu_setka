services:
  backend:
    image: ghcr.io/pgeyko/omsu_setka/backend:latest
    container_name: omsu_setka_backend
    restart: always
    volumes:
      - backend_data:/app/data
    environment:
      - ADMIN_KEY
      - CORS_ALLOWED_ORIGINS
      - FIREBASE_SERVICE_ACCOUNT
      - APP_BASE_URL
      - APP_ENV=production
      - SERVER_PORT=8080
      - SQLITE_PATH=/app/data/mirror.db
      - GOMEMLIMIT=192MiB
    mem_limit: 256m
    mem_reservation: 128m
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M
    networks:
      - nginx-network
    healthcheck:
      test: ["CMD", "curl", "-s", "-o", "/dev/null", "http://localhost:8080/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: ghcr.io/pgeyko/omsu_setka/frontend:latest
    container_name: omsu_setka_frontend
    restart: always
    depends_on:
      - backend
    networks:
      - nginx-network

networks:
  nginx-network:
    external: true

volumes:
  backend_data:
