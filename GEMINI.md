# Gemini Instructions

Use the shared project instructions instead of maintaining Gemini-specific
copies.

1. Read `AGENTS.md`.
2. Read `.agents/README.md`.
3. Load relevant skills from `.agents/skills/` according to the task.

`AGENTS.md` is the canonical source of truth for this repository.
